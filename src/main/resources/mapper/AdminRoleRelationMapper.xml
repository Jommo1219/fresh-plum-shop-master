<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jommo.mapper.AdminRoleRelationMapper">

    <resultMap id="BaseResultMap" type="com.jommo.domain.AdminRoleRelation">
            <id property="id" column="id" jdbcType="BIGINT"/>
            <result property="adminId" column="admin_id" jdbcType="BIGINT"/>
            <result property="roleId" column="role_id" jdbcType="BIGINT"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,admin_id,role_id
    </sql>
    <select id="getRoleList" resultMap="com.jommo.mapper.RoleMapper.BaseResultMap">
        select r.*
        from admin_role_relation ar
                 left join role r on ar.role_id = r.id
        where ar.admin_id = #{adminId}
    </select>
</mapper>
